

<div class="row col-equal-height">
    <div class="col-md-4">
        <div class="card tile" style="height: 200px;">
            <div class="card-body">
                Incomes
                {{#each data.income_count}}
                    <p>{{data.income_count}}</p>
                {{else}}
                    <p>{{data.income_count}}</p>
                    {{/each}}

                {{#each data.income_list}}
                    <p>{{user.first_name}} {{user.last_name}}</p>
                {{/each}}
            </div>
        </div>
    </div>
</div>
